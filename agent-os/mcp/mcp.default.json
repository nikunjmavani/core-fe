{
  "mcpServers": {
    "codegraph": {
      "command": "pnpm",
      "args": ["exec", "codegraph", "serve", "--mcp"]
    },
    "headroom": {
      "command": "uvx",
      "args": ["--from", "headroom-ai[mcp]", "headroom", "mcp", "serve"]
    }
  }
}
