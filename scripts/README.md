# Scripts (retired)

All project scripts moved to **`tooling/`** (aligned with core-be). See [tooling/README.md](../tooling/README.md).

Use `pnpm` scripts — e.g. `pnpm setup:local`, `pnpm health`, `pnpm github:sync` — not direct shell paths under this directory.
