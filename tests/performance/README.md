# Performance tests (optional)

Lighthouse CI, bundle-size assertions, or other front-end performance scenarios.

**Add here when:** You add performance gates (e.g. Lighthouse budget or `bundlesize`). Document the command in this README and in `tests/README.md` when you add specs.

**Tools:** Depends on what you add (Lighthouse CI, bundlesize, etc.).
