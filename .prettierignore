dist
coverage
node_modules
test-results
# pnpm owns the lockfile format — Prettier's YAML rules must never rewrite it
# (lint-staged's *.yaml pattern would otherwise format it on dependency commits).
pnpm-lock.yaml
# Vendored/agent-managed skills (same stance as .markdownlint-cli2.jsonc):
# ecosystem skills under agent-os/skills are hash-locked in agent-os/skills-lock.json
# against their UPSTREAM bytes — a Prettier rewrite (e.g. via lint-staged at commit)
# breaks the `pnpm agent-os:check` vendored-hash gate.
agent-os/skills/
